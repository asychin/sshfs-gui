# direnv configuration for SSHFS GUI
# This file automatically loads the Nix development environment

# Use nix-shell with shell.nix
use nix

# Optional: Add local bin to PATH for development
# PATH_add bin

# Optional: Set any environment variables needed for development
# export DEBUG=1
